Place your dashboard screenshot here (PNG/JPG). Recommended filename: `dashboard.png`.

To include the screenshot in the repo:

```powershell
git add Images/screenshots/dashboard.png
git commit -m "feat: add dashboard screenshot"
git push
```
